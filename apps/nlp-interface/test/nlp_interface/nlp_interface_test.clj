(ns nlp-interface.nlp-interface-test
  (:require [clojure.test :refer [deftest is testing]]
            [nlp-interface.nlp-interface :as sut])) ; system under test

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
