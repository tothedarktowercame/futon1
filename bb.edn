;; bb.edn
{:tasks
 {test:all
  (let [apps ["client"
              "nlp-interface"
              "api"
              "open-world-ingest"
              "common"
              "graph-memory"]]
    (doseq [a apps]
      (println "→ Testing" a)
      ;; run inside each app so its :test alias + deps are respected
      (shell {:dir (str "apps/" a)}
             "clojure -M:test -m cognitect.test-runner -d test")))

  lint (shell "clj-kondo --lint apps libs")
  demo (shell "cd apps/demo && clojure -M:run-m")}}
